# 过磅小程序 — 接口文档

> 来源：[Apifox 在线文档（过磅）](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6)  
> 索引：[llms.txt](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/llms.txt)  
> 整理时间：2026-06-04

## 环境说明

| 项 | 值 |
|---|---|
| 测试环境 Base URL | `https://wa15.shangyundian.cn/api/` |
| 图片访问前缀 | `https://wa15.shangyundian.cn/upload/`（项目 `utils/request.js` 中 `UPLOAD_BASE`） |
| 认证方式 | 请求头 `token`（登录接口返回；需登录的接口均必填） |
| 内容类型 | 除上传外，多数写接口为 `application/x-www-form-urlencoded`；上传为 `multipart/form-data` |

### 通用响应约定（与前端封装一致）

前端 `utils/request.js` 判定成功：`code === 0` 或 `code === 200` 或无 `code` 字段；业务数据通常在 `data` 中。

---

## 接口一览

| # | 名称 | 方法 | 路径 | 需 token | Apifox |
|---|------|------|------|----------|--------|
| 1 | 上传图片 | POST | `/upload` | 是 | [文档](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/468051933e0) |
| 2 | 获取网站设置 | GET | `/siteConfig` | 是 | [文档](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/468052116e0) |
| 3 | 拉起支付 | GET | `/goPay` | 是 | [文档](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/468052342e0) |
| 4 | 登录（仅测试） | POST | `/login` | 是* | [文档](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/468053096e0) |
| 5 | 创建项目 | POST | `/createProject` | 是 | [文档](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/468058622e0) |
| 6 | 项目分配 | POST | `/allocateProject` | 是 | [文档](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/468059164e0) |
| 7 | 项目详情 | GET | `/projectInfo` | 是 | [文档](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/468060014e0) |

\* 登录接口文档中 Header 仍标了 `token`，以 Apifox 为准；业务说明为只传 `phone` 即可登录并返回 token。

### 代码中已调用、但当前 Apifox 分享未包含

以下路径在 `utils/request.js` 的 `api` 对象里已有封装，**不在上述 7 个分享接口中**，对接时需与后端确认是否已上线或另有文档：

| 方法 | 路径 | 前端方法 |
|------|------|----------|
| GET | `/sites` | `api.getSites()` |
| GET | `/myProject` | `api.myProjects()` |
| POST | `/updateHairInfo` | `api.updateHairInfo()` |
| POST | `/updateReceiveInfo` | `api.updateReceiveInfo()` |

---

## 业务流程（Apifox 创建项目说明）

1. 调用 **创建项目** → 返回 `order_no`、`id`
2. 用 `order_no` 调用 **拉起支付**
3. **轮询项目详情**，支付完成后进入下一步
4. 调用 **项目分配** 提交分配比例

---

## 1. 上传图片

- **POST** `https://wa15.shangyundian.cn/api/upload`
- **Content-Type**：`multipart/form-data`

### 请求

| 位置 | 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| Header | `token` | string | 是 | 登录 token |
| Body | `thumb` | file | 是 | 图片文件 |

### 响应

- HTTP 200，`application/json`（具体字段以实际返回为准；前端取 `data.path` 拼 `UPLOAD_BASE` 展示）

---

## 2. 获取网站设置

- **GET** `https://wa15.shangyundian.cn/api/siteConfig`

### 请求

| 位置 | 参数 | 类型 | 必填 |
|------|------|------|------|
| Header | `token` | string | 是 |

### 响应

- HTTP 200，JSON（结构见 Apifox / 联调返回）

---

## 3. 拉起支付

- **GET** `https://wa15.shangyundian.cn/api/goPay`

### 请求

| 位置 | 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| Query | `order_no` | string | 是 | 创建项目返回的订单号 |
| Header | `token` | string | 是 | |

---

## 4. 登录【仅测试，后续会改】

- **POST** `https://wa15.shangyundian.cn/api/login`
- **Content-Type**：`application/x-www-form-urlencoded`

### 说明

只需传 `phone`，不需要注册、不需要密码，直接登录并返回 token。

### 请求

| 位置 | 参数 | 类型 | 必填 |
|------|------|------|------|
| Header | `token` | string | 是（文档标注，以实现为准） |
| Body | `phone` | string | 是 |

---

## 5. 创建项目【登录】

- **POST** `https://wa15.shangyundian.cn/api/createProject`
- **Content-Type**：`application/x-www-form-urlencoded`

### 说明

返回 **订单号 `order_no`** 与 **`id`**。后续用 `order_no` 拉起支付，轮询详情确认已支付后再做项目分配。

### 请求 Body

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `title` | string | 是 | 项目名称 |
| `spec` | string | 是 | 规格（JSON 字符串，格式保持不变） |
| `master_info` | string | 是 | 项目负责人（JSON 字符串） |
| `shipper_company` | string | 是 | 发货单位全称 |
| `receiver_company` | string | 是 | 卸货单位全称 |
| `shipper_info` | string | 是 | 发货人员（JSON 字符串） |
| `receiver_info` | string | 是 | 收货人员（JSON 字符串） |
| `price` | string | 是 | 司机支付金额 |
| `need_ticket` | string | 否 | 需要发票时传值，不需要则不传 |
| `ticket_info` | string | 否 | 发票信息 JSON 字符串，如 `{"抬头":"xxx"}` |

| Header | `token` | 是 |

### 与当前小程序差异

`pages/project/allocate.vue` 现用 JSON POST 字段名 `driver_fee`、`allocate` 等；Apifox 为 **form** 且司机金额为 **`price`**，发票为 **`need_ticket` / `ticket_info`**。联调时需按后端实际字段对齐。

---

## 6. 项目分配【登录】

- **POST** `https://wa15.shangyundian.cn/api/allocateProject`
- **Content-Type**：`application/x-www-form-urlencoded`

### 说明

文档注明：**此接口会有一点变动**。

### 请求 Body

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `order_id` | string | 是 | 项目 id |
| `allocate` | string | 是 | 分配比例，JSON 字符串（格式保持不变） |

| Header | `token` | 是 |

### 与当前小程序差异

创建流程目前在 `createProject` 一次提交中带 `allocate`；Apifox 为 **独立接口** `allocateProject`，且参数为 `order_id`。

---

## 7. 项目详情【登录】

- **GET** `https://wa15.shangyundian.cn/api/projectInfo`

### 请求

| 位置 | 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| Query | `project_id` | string | 是 | 项目 ID |
| Header | `token` | string | 是 | |

### 响应

- 前端 `upload.vue` / `project.vue` 使用 `res.row`（如 `driver_fee`、`title` 等），与 Apifox 空 schema 不一致，以联调为准。

---

## 附录：前端封装对照（`utils/request.js`）

```javascript
// BASE_URL = 'https://wa15.shangyundian.cn/api'
api.getSites()           // GET  /sites          — 分享文档未列出
api.createProject()      // POST /createProject  — 见上文，注意 form + price
api.myProjects()         // GET  /myProject      — 分享文档未列出
api.projectInfo()        // GET  /projectInfo    — 见上文
api.updateHairInfo()     // POST /updateHairInfo — 分享文档未列出
api.updateReceiveInfo()  // POST /updateReceiveInfo — 分享文档未列出
uploadFile()             // POST /upload         — 见上文，字段 thumb
```

---

## 前端页面对接（已实现）

| 接口 | 页面 / 模块 |
|------|-------------|
| `POST /login` | `pages/auth/login` |
| `GET /siteConfig` | `App.vue` 启动拉取；`pages/mine/service` 展示热线等 |
| `POST /upload` | `utils/request.js` `uploadFile`；`driver/upload`、`driver/pay` |
| `POST /createProject` | `pages/project/fee`（第 3 步） |
| `GET /goPay` | `pages/project/fee` |
| 轮询 `GET /projectInfo` | `pages/project/fee`（`pollProjectPaid`） |
| `POST /allocateProject` | `pages/project/allocate`（第 4 步） |
| `GET /projectInfo` | `driver/upload`、`driver/project`、`print/detail` |
| `GET /myProject` | `pages/print/print` |
| `POST /updateHairInfo` / `updateReceiveInfo` | `driver/upload`、`driver/pay`（Apifox 未列，字段待联调确认） |

请求层：`utils/request.js`（Header `token`、`postForm`）；建项参数：`utils/api-util.js` `buildCreateProjectPayload`。

---

## 更新文档方式

Apifox 分享根目录若增删接口，可访问：

`https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/llms.txt`

获取最新接口列表后，再让我同步更新本文件。
