# 过磅小程序 — 接口文档

> 来源：[Apifox 在线文档（过磅）](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6)  
> 索引：[llms.txt](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/llms.txt)（共 **17** 个接口，2026-06 同步）

## 环境说明

| 项 | 值 |
|---|---|
| Base URL | `https://wa15.shangyundian.cn/api`（`utils/request.js` → `BASE_URL`） |
| 图片前缀 | `https://wa15.shangyundian.cn/upload/`（`UPLOAD_BASE`） |
| 认证 | 请求头 `token`（`POST /login` 返回，写入 `gb_user.token`） |
| 成功码 | `code === 0` 或 `200`；业务数据在 `data` |

---

## 接口总览

| # | 名称 | 方法 | 路径 | 前端封装 | 说明 |
|---|------|------|------|----------|------|
| 1 | 上传图片 | POST | `/upload` | `uploadFile` | multipart，`thumb` |
| 2 | 获取网站设置 | GET | `/siteConfig` | `api.siteConfig` | |
| 3 | 拉起支付 | GET | `/goPay` | `api.goPay` | query：`order_no` |
| 4 | 登录 | POST | `/login` | `api.login` | form：`phone` |
| 5 | 创建项目 | POST | `/createProject` | `api.createProject` | **JSON** |
| 6 | 项目分配 | POST | `/allocateProject` | `api.allocateProject` | form |
| 7 | 项目详情 | GET | `/projectInfo` | `api.projectInfo` | query：`project_id` |
| 8 | 导出简洁版 | GET | `/exportSimple` | `api.exportSimple` | query：`project_id` |
| 9 | 导出详细版 | GET | `/exportDetail` | `api.exportDetail` | query：`project_id` |
| 10 | 更新发货站信息 | POST | `/updateHairInfo` | `api.updateHairInfo` | 文档：原参数 |
| 11 | 更新收货站信息 | POST | `/updateReceiveInfo` | `api.updateReceiveInfo` | 文档：原参数 |
| 12 | 确认发货站数据 | POST | `/correctHairInfo` | `api.correctHairInfo` | form，验票纠错 |
| 13 | 确认收货站信息 | POST | `/correctReceiveInfo` | `api.correctReceiveInfo` | 参数同 #12 |
| 14 | 打分 | POST | `/evaluate` | `api.evaluate` | form：`project_id`、`type`（`s`/`r`）、`score`（1–5） |
| 15 | 确认计件数量 | POST | `/correctResultNum` | `api.correctResultNum` | form：`project_id`、`result_num` |
| 16 | 获取用户信息 | GET | `/getUserInfo` | `api.getUserInfo` | |
| 17 | 我的项目 | GET | `/myProject` | `api.myProjects` | |

**代码遗留（不在当前 Apifox 分享）**：`GET /sites` → `api.getSites()`，页面未使用。

辅助：`pollProjectPaid`、`buildCreateProjectPayload`、`invokeWechatPay`、`parseSiteConfig`、`getProjectPrice`。

---

## 业务流程

### 创建项目

1. `POST /createProject` → `order_no`、`id`
2. `GET /goPay?order_no=` → `uni.requestPayment`
3. 轮询 `GET /projectInfo`
4. `POST /allocateProject`

### 司机上传磅单

1. `POST /upload`（磅单图、地址图）
2. `POST /updateHairInfo`（发货）或 `POST /updateReceiveInfo`（收货）

### 完整过磅流程（前端已实现）

1. 负责人 `createProject` → `goPay` → `allocateProject`
2. 司机 `updateHairInfo`（发货磅单）→ `updateReceiveInfo`（收货磅单）
3. 司机 `POST /evaluate`（发货/收货服务评分）
4. 发货过磅人 `correctHairInfo`；收货过磅人 `correctReceiveInfo`
5. 负责人 `correctResultNum`（确认计件数量）
6. 负责人 `exportSimple` / `exportDetail` 打印对账单

### 打印对账单

- 简洁版：`GET /exportSimple`
- 详版：`GET /exportDetail`
- 专业分析：Apifox 暂无独立导出接口，当前 `print/detail?mode=pro` 走本地汇总

---

## 1. 上传图片 `POST /upload`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/468051933e0)

| 位置 | 参数 | 说明 |
|------|------|------|
| Header | `token` | |
| Body | `thumb` | 图片文件 |

响应：取 `data.path`，`imgUrl()` 拼完整地址。页面：`driver/upload`、`driver/pay`。

---

## 2. 网站设置 `GET /siteConfig`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/468052116e0)

Header：`token`。

### 响应（已联调）

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "data": {
      "one_price": "3",
      "project_price": "100"
    }
  }
}
```

| 字段 | 说明 |
|------|------|
| `one_price` | 司机单票价格（待页面对接） |
| `project_price` | 项目创建服务费；`pages/project/fee` 展示与支付判断 |

---

## 3. 拉起支付 `GET /goPay`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/468052342e0)

| Query | `order_no` | 创建项目返回的订单号 |
| Header | `token` | |

前端：`invokeWechatPay` 调起微信小程序支付（`timeStamp`、`nonceStr`、`package`、`signType`、`paySign`）。

---

## 4. 登录 `POST /login`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/468053096e0)

- form：`phone`
- 说明：仅测试环境，只传手机号，返回 token

### 响应（已联调）

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "token": "EH5X8yNJyzt2g3QKy7A3UzqStos2fBtgNmbhhTU3B1Vv68Fi1lxyWz0nHeGV"
  }
}
```

页面：`pages/auth/login`。

---

## 5. 创建项目 `POST /createProject`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/468058622e0)

- **Content-Type**：`application/json`
- 返回：`order_no`、`id`

| 参数 | 类型 | 说明 |
|------|------|------|
| `title` | string | 项目名称 |
| `spec` | array | 规格列表 |
| `master_info` | array | 负责人 `{ name, phone }[]` |
| `shipper_company` / `receiver_company` | string | 单位全称 |
| `shipper_info` / `receiver_info` | array | 人员列表 |
| `price` | number | 司机缴费档位金额 |
| `need_ticket` | number | 需要发票传 `1` |
| `ticket_info` | object | `{ "抬头":"","税号":"" }` |

页面：`pages/project/fee`（`buildCreateProjectPayload`）。

---

## 6. 项目分配 `POST /allocateProject`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/468059164e0)

- form；文档注明接口可能变动

| 参数 | 说明 |
|------|------|
| `order_id` | 项目 id |
| `allocate` | 分配比例 JSON 字符串 |

页面：`pages/project/allocate`。

---

## 7. 项目详情 `GET /projectInfo`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/468060014e0)（同 [470292116e0](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/470292116e0)）

| Query | `project_id` |
| Header | `token` |

前端常用 `data.row`（含 `hair` / `receive`、重量、图片等）。页面：`driver/upload`、`driver/project`、`print/detail`、`fee` 轮询。

---

## 8. 导出简洁版 `GET /exportSimple`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/469998206e0)

| Query | `project_id`（必填） |
| Header | `token` |

页面：`print/detail?mode=simple`（优先远端，失败回退本地 `db`）。

---

## 9. 导出详细版 `GET /exportDetail`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/469998710e0)

| Query | `project_id`（必填） |
| Header | `token` |

页面：`print/detail?mode=detail`（优先远端，失败回退本地 `db`）。

---

## 10. 更新发货站信息 `POST /updateHairInfo`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/469999787e0)

- 文档说明：**原参数**（Apifox 未展开 schema）
- Header：`token`

当前前端提交（form）：

`project_id`、`weight_pic`、`addr_pic`、`number`、`gross`、`tare`、`net`

页面：`driver/upload`、`driver/pay`（发货磅单）。

---

## 11. 更新收货站信息 `POST /updateReceiveInfo`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/469999938e0)

- 文档说明：**原参数**
- 参数同 #10，用于收货磅单

页面：`driver/upload`、`driver/pay`。

---

## 12. 确认发货站数据 `POST /correctHairInfo`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/470001934e0)

- form

| 参数 | 必填 | 说明 |
|------|------|------|
| `project_id` | 是 | |
| `gross` | 是 | 毛重 |
| `tare` | 是 | 皮重 |
| `net` | 是 | 净重 |

对应业务：发货过磅人验票 / 纠错。页面：`verify/detail`。

---

## 13. 确认收货站信息 `POST /correctReceiveInfo`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/470004474e0)

- 文档：**参数同确认发货站**（`project_id`、`gross`、`tare`、`net`）
- 对应收货过磅人验票。页面：`verify/detail`。

---

## 14. 打分 `POST /evaluate`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/470008180e0)

| 字段 | 说明 |
|------|------|
| `project_id` | 项目编号 |
| `type` | `s` 发货过磅 / `r` 收货过磅 |
| `score` | 1–5 星 |

页面：`project/rate`（司机上传收货磅单后跳转）。

---

## 15. 确认计件数量 `POST /correctResultNum`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/470008791e0)

- form

| 参数 | 必填 | 说明 |
|------|------|------|
| `project_id` | 是 | |
| `result_num` | 是 | 最终计件数量（手输） |

页面：`mine/projectDetail`（负责人确认计件）。

---

## 16. 获取用户信息 `GET /getUserInfo`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/470056377e0)

- Header：`token`
- 响应 schema 为空，联调后补充

页面：`mine/mine`（`onShow` 刷新用户资料）。

---

## 17. 我的项目 `GET /myProject`

[Apifox](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/470292324e0)

- Header：`token`
- 响应：`data.list[]`，每项含当前用户在该项目中的 `role` / `role_label`

主要字段（联调样例）：

| 字段 | 说明 |
|------|------|
| `id` | 项目编号 |
| `title` | 项目名称 |
| `uid` | 发布人用户 id |
| `role` | 当前用户角色，如 `creator` |
| `role_label` | 角色文案，如 `发布人` |
| `master` | 负责人数组 `{ name, phone, master_uid }` |
| `shipper` | 发货人员 `{ name, phone, shipper_uid }` |
| `receiver` | 收货人员 `{ name, phone, receiver_uid }` |
| `shipper_status` / `receiver_status` | 发货/收货核对状态 |
| `result_num` | 已确认计件数量 |
| `pay_status` | 支付状态 |

前端：`parseMyProjectList`、`isProjectOwner`（负责人）、`weighRolesInProject`（过磅人）。

页面：`mine/projects`、`print/print`（负责人）；`join/joined`（发货/收货人员）。

---

## 页面对照（汇总）

| 页面 | 相关接口 |
|------|----------|
| `auth/login` | `POST /login` |
| `App.vue`、`mine/service` | `GET /siteConfig` |
| `project/fee` | `createProject`、`goPay`、轮询 `projectInfo`、`siteConfig` |
| `project/allocate` | `allocateProject` |
| `driver/upload`、`driver/pay` | `upload`、`updateHairInfo`、`updateReceiveInfo`、`projectInfo` |
| `driver/project` | `projectInfo` |
| `project/rate` | `evaluate` |
| `verify/list`、`verify/detail` | `projectInfo`、`correctHairInfo`、`correctReceiveInfo` |
| `join/joined`、`join/joinedDetail` | `myProjects`、`projectInfo` |
| `print/print` | `myProjects` |
| `print/detail` | `exportSimple`、`exportDetail`、`projectInfo`、本地 `db` 回退 |
| `mine/mine` | `getUserInfo` |
| `mine/projects` | `myProjects` |
| `mine/projectDetail` | `projectInfo`、`correctResultNum` |

---

## 更新方式

1. 访问 [llms.txt](https://s.apifox.cn/b53fa605-25b2-4ebf-9fcf-f0314090c0c6/llms.txt) 看是否有新增接口  
2. 打开各 `.md` 链接核对 path / 参数  
3. 同步本文件与 `utils/request.js` 的 `api` 对象
